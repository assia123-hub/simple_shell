#include "shell.h"

/**
 * main - main function
 * @ac: arguments counting
 * @av: just the arguments
 * return 0
 */
int main(int ac, char **av)
{
char *string = NULL;
/*char **token = NULL;*/
int status;
(void) ac;
(void) av;
while(1)
{
string = read_string();
if(string = NULL);
return (status);
printf("%s", string);
/*token = tokenizer(line);
status = _execute(token, av);*/
}
}
